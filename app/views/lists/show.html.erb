<div class="col-md-4">
  <p id="notice"><%= notice %></p>

  <div class="list_header">
    <h4><%= @list.name %></h4>
  </div>

  <div class="list_body">

  <%= render partial: 'entries/list-entries' %>

    <div class="actions">
      <%= link_to 'Edit', edit_list_path(@list) %> |
      <%= link_to 'Back', lists_path %>
    </div>
  </div>

</div>
